package org.FullyAbstraction;

public interface  Company {
	
	// Interface 
	
       void companyName() ;
	
	   void companyId() ;
	
	   void CompanyLocation();
	   
	// 1. In interface we called as fully abstraction    
   	// 2. we can't be create object 
    //3.  use a keyword interface in class only
	// 4. contains only abstract method 
    //5.  her we can achieve multiple inheritance by using the keyword
	 // Implements 
	 // 6.use a keyword Implements 
	 // It should be default keyword as public   

}
